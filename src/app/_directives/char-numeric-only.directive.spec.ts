import { CharNumericOnlyDirective } from './char-numeric-only.directive';

describe('CharNumericOnlyDirective', () => {
  it('should create an instance', () => {
    const directive = new CharNumericOnlyDirective();
    expect(directive).toBeTruthy();
  });
});
